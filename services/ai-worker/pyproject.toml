[project]
name = "story-platform-ai-worker"
version = "0.1.0"
description = "Python AI worker shell for NovelWriter"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
story-platform-ai-worker = "story_platform_ai.main:main"

[tool.pytest.ini_options]
pythonpath = ["src", "../../packages/contracts/python"]
testpaths = ["tests"]
