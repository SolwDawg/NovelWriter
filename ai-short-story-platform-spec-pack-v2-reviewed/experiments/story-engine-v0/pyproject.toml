[project]
name = "story-engine-v0"
version = "0.1.0"
description = "Deterministic evaluation harness for the AI Story Engine V0 proof"
requires-python = ">=3.11"
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=8.0"]

[project.scripts]
story-v0 = "story_v0.eval_runner:main"

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]

